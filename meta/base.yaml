- defaults:
    link:
      create: true
      relink: true
      force: true
    shell:
      stdout: true
      stderr: true

- create:
    - $HOME/.config

- clean:
    $HOME:
      force: true
    $HOME/.config:
      recursive: true
